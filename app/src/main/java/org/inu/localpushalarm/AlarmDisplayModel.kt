package org.inu.localpushalarm

data class AlarmDisplayModel(
    var onOff: Boolean

) {
//    viewModel 로 이동~
//    val onOffText = if (onOff) "알람 취소하기" else "알람 받기"   // alarmOnOff textView(text)
//    val onOffColor = if (onOff) R.color.background else R.color.primary   // alarmOnOff textView(textColor)
//    val onOffBackground = if (onOff) R.color.primary else R.color.background  // alarmOnOff textView(background)
}